# commands

## create new subdomain

```sh
deno run --allow-all cli/create-subdomain.ts <SubdomainName>
# or make it executable and run directly:
./create-subdomain.ts <SubdomainName>
```

## pending tasks

1. create all globalusings in tests projects
2. create github actions (build an run tests)
3. add husky
4. gestión global de paquetería y versión de .net
5. crear por comandos los ficheros de solución, que ahora no me gustan cómo están